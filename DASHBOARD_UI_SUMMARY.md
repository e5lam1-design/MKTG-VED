# 🎨 دليل وتصميم واجهة المستخدم (UI/UX Interactive Architecture)
### لوحة تحكم إدارة إنتاج ومونتاج الفيديوهات التسويقية | Marketing Video Dashboard
> **التقنيات المبني عليها النظام:** React 19 / Next.js Architecture • TypeScript • Tailwind CSS • Lucide Icons • Supabase Realtime

---

## 📑 الفهرس العام
1. [الهوية البصرية وفلسفة التصميم (Theme & Design System)](#1-الهوية-البصرية-وفلسفة-التصميم)
2. [الهيكل العام للشاشة (Layout Architecture)](#2-الهيكل-العام-للشاشة)
3. [الشريط العلوي التفاعلي (Interactive Top Header)](#3-الشريط-العلوي-التفاعلي)
4. [شريط التبويبات والمراحل (Tabs & Navigation System)](#4-شريط-التبويبات-والمراحل)
5. [شريط الفلاتر السريعة ومسبقات العرض (Filters & Preset Bar)](#5-شريط-الفلاتر-السريعة-ومسبقات-العرض)
6. [مكون شريط التنبيهات والتوجيهات الإدارية (Page Announcement Bar)](#6-مكون-شريط-التنبيهات-والتوجيهات-الإدارية)
7. [الشاشات وطرق العرض الرئيسية (Core Views)](#7-الشاشات-وطرق-العرض-الرئيسية)
   - [أ. الصفحة الرئيسية (Home Dashboard)](#أ-الصفحة-الرئيسية-home-dashboard)
   - [ب. جدول التجميعات المركزي (Tagme3at Hub)](#ب-جدول-التجميعات-المركزي-tagme3at-hub)
   - [ج. جداول المراحل الإنتاجية (Stage Data Grids)](#ج-جداول-المراحل-الإنتاجية-stage-data-grids)
   - [د. مسار الريلز (Reels Production Pipeline)](#د-مسار-الريلز-reels-production-pipeline)
   - [هـ. إدارة المستخدمين والصلاحيات (User Management)](#هـ-إدارة-المستخدمين-والصلاحيات-user-management)
8. [النوافذ المنبثقة التفاعلية (Interactive Modals & Drawers)](#8-النوافذ-المنبثقة-التفاعلية)
9. [مكونات الإدخال والتعديل اللحظي (Interactive Form Controls)](#9-مكونات-الإدخال-والتعديل-اللحظي)
10. [دليل تحويل المكونات إلى Next.js App Router](#10-دليل-تحويل-المكونات-إلى-nextjs-app-router)

---

## 1. الهوية البصرية وفلسفة التصميم
* **النمط العام:** Dark Glassmorphism عصري فخم مستوحى من واجهات Linear و Vercel.
* **الخلفيات:** تدرجات داكنة عميقة (`bg-slate-950` / `bg-[#0b0f19]`) مع طبقات تأثير الزجاج البلوري الضبابي (`backdrop-blur-xl bg-white/[0.03] border-white/[0.08]`).
* **الألوان الدلالية:**
  - **الأزرق والنيلي (`#3b82f6`):** للعمليات القياسية وحالة الربط والبيانات الأساسية.
  - **البنفسجي المتوهج (`#8b5cf6`):** للعلامة التجارية وعمليات التجميعات والأولويات.
  - **الأخضر الزمردي (`#10b981`):** للمهام المكتملة، التواجد اللحظي (Online)، والنشر الناجح.
  - **البرتقالي الكهرماني (`#f59e0b`):** للتنبيهات، المهام قيد التنفيذ، وحدود السعة القصوى.
  - **الوردي الفاقع والأحمر (`#f43f5e`):** للمهام العاجلة، الأخطاء، وإلغاء المهام.

---

## 2. الهيكل العام للشاشة
```
┌────────────────────────────────────────────────────────────────────────┐
│ 1. Interactive Top Header (Logo, Search, Live Presence, User, Actions)  │
├────────────────────────────────────────────────────────────────────────┤
│ 2. Sticky Tab Bar & Pinned Shortcuts (Home, Tagme3at, Stages, Reels)  │
├────────────────────────────────────────────────────────────────────────┤
│ 3. Page Announcement & Directive Banner (Collapsible Alert Card)       │
├────────────────────────────────────────────────────────────────────────┤
│ 4. Filter Toolbar & Quick Preset Controls (Teacher, Year, My Tasks)    │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│ 5. Active View Canvas (Home / Data Grid / Pipeline / Users)            │
│    - KPI Metrics Cards                                                 │
│    - Interactive Table with Realtime Rows                              │
│    - Floating Action Drawers & Pagination Controls                     │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 3. الشريط العلوي التفاعلي
يقع في أعلى الشاشة ويحتوي على أزرار وتحكمات مركزية:

| العنصر | النوع التفاعلي | الوظيفة والسلوك |
| :--- | :--- | :--- |
| **شعار التطبيق والعنوان** | `Link / Button` | العودة للشاشة الرئيسية مع مؤشر نبضي يوضح حالة المزامنة اللحظية (Realtime Connected). |
| **شريط البحث الشامل (Global Search)** | `Input + Shortcut (Ctrl+K)` | فلترة لحظية بالاسم، الكود، اسم المدرس، أو المونتير عبر كامل الشيت الحالي. |
| **أيقونة التحديث اليدوي (Refresh)** | `Icon Button` | دوران أنيميشن خفيف أثناء جلب أحدث البيانات مع توليد توست نجاح (`Toast`). |
| **مكدس المتواجدين (Online Presence Stack)** | `Avatar Stack + Popover` | عرض صور/أفاتار الأعضاء المتصلين بالداشبورد حالياً مع نقطة خضراء؛ والضغط عليه يفتح قائمة بأسماء المتواجدين وأدوارهم. |
| **زر ملء الشاشة (Fullscreen Toggle)** | `Icon Button` | تكبير الواجهة للشاشات الكبيرة وغرف المونتاج. |
| **حالة التيليجرام (Telegram Status Badge)** | `Interactive Badge` | يظهر أيقونة تيليجرام خضراء لو الحساب مربوط، أو رمادية تفتح نافذة الربط بـ QR Code. |
| **قائمة الملف الشخصي (User Profile Dropdown)** | `Dropdown Menu` | صورة المستخدم، الرتبة (Admin, Manager, Editor, Junior)، خيارات الحساب، وتبديل تسجيل الخروج. |

---

## 4. شريط التبويبات والمراحل
نظام تنقل أفقي تفاعلي مرن يدعم التثبيت والتمرير السلس:

### المجموعات الرئيسية للتبويبات:
1. **الرئيسية (Home):** لوحة القيادة المركزية والإحصائيات التراكمية.
2. **التجميعات (Tagme3at):** المركز الموحد لكل المهام وتوزيعها بين الأقسام.
3. **مراحل الإنتاج (Stages S1, S2, M1, M2...):** جداول مخصصة لكل مرحلة تصوير ومونتاج.
4. **مسار الريلز (Reels Pipeline):** شيتات التصوير (Shooting)، التقطيع (Cuts)، المونتاج البصري (VE).
5. **إدارة الفريق والصلاحيات:** للأدمن والمانجر فقط.

### أزرار وتفاعلات التبويب:
* **زر التثبيت (`Pin Tab 📌`):** أيقونة دبوس صغيرة على كل تبويب تمكن العضو من تثبيت مراحله المفضلة لتظهر في شريط وصول سريع دائم.
* **شارة العدد المتبقي (`Uncompleted Count Badge`):** دائرة صغيرة متوهجة بجانب اسم كل مرحلة تعرض عدد الفيديوهات المتبقية وغير المنجزة (مثلاً: `🔥 14`).
* **شريط السعة والأولويات (`Priority Limit Stepper`):** زر تفاعلي يسمح للمشرف برفع أو خفض سقف المهام العاجلة لتلك المرحلة لحظياً.

---

## 5. شريط الفلاتر السريعة ومسبقات العرض
شريط أدوات وظيفي يحتوي على عناصر تصفية دقيقة:

* **زر "مهامي فقط" (`Toggle Button - My Tasks Only`):**
  - زر بلمسة واحدة يفلتر كل الصفوف ليعرض فقط المهام المسندة للمستخدم المسجل حالياً، مما يوفر على المونتير عناء البحث.
* **قائمة منسدلة لاختيار المدرس (`Teacher Filter Select`):**
  - قائمة غنية مع صور وأسماء المدرسين لتصفية مهام مدرس بعينه.
* **فلتر السنة الدراسية (`Grade/Year Pills`):**
  - كبسولات تفاعلية (أولى ثانوي، ثانية، ثالثة، إعدادي).
* **فلتر حالة المهمة (`Status Pills`):**
  - (الكل، جاهز للتصوير، تم التصوير، قيد المونتاج، جاهز للمراجعة، مكتمل).
* **مسبقات العرض المحفوظة (`Custom Presets Manager`):**
  - زر يتيح للمستخدم حفظ مجموعة فلاتر مخصصة وتسميتها (مثلاً: "فيديوهات مستر أحمد تالتة ثانوي العاجلة") واسترجاعها بنقرة واحدة.

---

## 6. مكون شريط التنبيهات والتوجيهات الإدارية
بطاقة علوية ديناميكية (`PageAnnouncementBar`) تظهر التوجيهات للمرحلة الحالية:

* **أنماط البطاقة المرئية بحسب النوع:**
  - `info`: زرقاء هادئة مع أيقونة معلومات.
  - `warning`: برتقالية للتحذيرات والمواعيد النهائية.
  - `alert`: حمراء متوهجة مع أيقونة مكبر صوت متحركة (`Animate-Bounce`) للتعليمات الطارئة.
  - `success`: خضراء زمردية للتهنئة بالإنجاز وتسليم المشاريع.
* **أزرار التحكم والتفاعل:**
  - **زر الطي والتوسيع (`Collapse/Expand`):** لتصغير الشريط لشريط رفيع حتى لا يزعج مساحة العمل.
  - **زر التعديل والنشر للمانجر (`Edit Directive`):** يفتح نافذة منبثقة للمانجر لكتابة التوجيه واختيار اللون وحفظه ليظهر لجميع أعضاء الفريق في الحال.
  - **زر الحذف (`Delete`):** لإزالة التنبيه عند انتهاء الغرض منه.

---

## 7. الشاشات وطرق العرض الرئيسية

### أ. الصفحة الرئيسية (Home Dashboard)
* **بطاقات المؤشرات الرقمية (KPI Metric Cards):**
  - 4 بطاقات زجاجية ثلاثية الأبعاد بإحصائيات حية: (إجمالي المهام، قيد المونتاج، الجاهزة للمراجعة، المنشورة على المنصات).
* **مربع ربط بوت التيليجرام الذكي (Telegram Bot Integration Widget):**
  - يعرض حالة الربط مع البوت.
  - زر فوري لتوليد كود الـ QR للربط بحساب التيليجرام.
  - زر تجربة الإشعار ("إرسال إشعار تجريبي 🚀").
* **مخطط السعة ومعدل الإنجاز اليومي:** رسوم بيانية توضح حركة تسليم الفيديوهات بالساعة واليوم.

---

### ب. جدول التجميعات المركزي (Tagme3at Hub)
* **الجدول التفاعلي (Interactive Data Grid):**
  - تمييز لوني لكل صف بحسب الأولوية والحالة.
  - خلايا تعديل سريعة للنصوص والملاحظات.
* **مكون إدخال الملاحظات والتاريخ (`HistoryInput`):**
  - مربع إدخال ذكي يتمتع بـ Debounce يمنع الحفظ العشوائي ويسجل اسم الكاتب وتاريخ التعديل تلقائياً.
* **أزرار الترحيل الفوري للمراحل (`Stage Transfer Buttons`):**
  - زر قائمة منسدلة لكل صف يسمح بنقل المهمة إلى (مرحلة S1، S2، المونتاج النهائي، أو الريلز) بضغطة زر مع إشعار توست فوري.

---

### ج. جداول المراحل الإنتاجية (Stage Data Grids)
* **رأس الجدول الثابت (`Sticky Header`):** يظل ظاهراً أثناء التمرير العمودي.
* **خانات التأكيد التفاعلية (`Action Checkboxes`):**
  - خانة "تم التصوير" (Filmed).
  - خانة "تم المونتاج" (Edited).
  - خانة "المراجعة الفنية" (Edit Check).
  - خانة "جاهز للنشر" (Publish Ready).
* **أزرار روابط جوجل درايف (`Drive Link Launchers`):**
  - زر أيقونة درايف زرقاء لفتح مجلد الـ Raw (الخامات الأصلية).
  - زر أيقونة درايف خضراء لفتح الـ Final Export (النسخة النهائية الجاهزة).
* **محدد المونتير والمسؤول (`Editor Selector Dropdown`):**
  - قائمة منسدلة لاختيار وتعيين المحرر المسؤول عن الفيديو.

---

### د. مسار الريلز (Reels Production Pipeline)
* مصمم خصيصاً للمحتوى القصير (Shorts / Reels / TikTok):
  - تبويب تصوير الريلز (Shooting).
  - تبويب تقطيع المقاطع (Cuts).
  - تبويب المونتاج البصري (VE - Video Editing).
  - مزامنة تلقائية: أي فيديو يتم تعليمه "تم التصوير" في الشوتينج ينتقل فوراً وبسلاسة إلى جدول الـ VE.

---

### هـ. إدارة المستخدمين والصلاحيات (User Management)
* شاشة إدارية للمدراء فقط:
  - قائمة جدولية بجميع أعضاء الفريق وصورهم وحساباتهم.
  - محدد الصلاحيات (`Role Badge Selector`): (Admin, Manager, Editor, Junior).
  - تبديل تفعيل/تعطيل الحساب (`Active Toggle Switch`).
  - تعيين معرف التيليجرام يدوياً أو عبر QR.

---

## 8. النوافذ المنبثقة التفاعلية

### 1. نافذة ربط التيليجرام عبر QR (`TelegramQrModal`):
* نافذة فخمة داكنة بتأثير بلوري تحتوي على:
  - كود QR ديناميكي جاهز للمسح بكاميرا الهاتف لفتح البوت وكتابة `/start`.
  - مؤشر تحميل نابض يفحص نجاح الاتصال.
  - رسالة نجاح خضراء تحتفل فور الربط مع تأثير confetti.

### 2. نافذة نشر التوجيهات والإعلانات (`EditAnnouncementModal`):
* حقل كتابة متعدد الأسطر مع خيارات الأنماط الأربعة.
* أزرار الاختيار السريع للون والأيقونة.
* زر الحفظ والتطبيق الفوري عبر السيرفر.

### 3. شريط الإشعارات والتوست (`Toast Notifications System`):
* إشعارات تفاعلية تظهر في أسفل يمين الشاشة مع أيقونات ملونة توضح:
  - حفظ التعديلات بنجاح.
  - وصول تنبيه إداري جديد من المانجر.
  - تحذيرات تجاوز سعة المرحلة.

---

## 9. مكونات الإدخال والتعديل اللحظي

```tsx
// نموذج لمكون بطاقة الإحصائيات (StatCard) بأسلوب Tailwind & Next.js
export function StatCard({ title, count, icon: Icon, trend, colorScheme }: StatCardProps) {
  return (
    <div className="relative overflow-hidden rounded-2xl border border-white/[0.08] bg-white/[0.03] p-5 backdrop-blur-xl transition-all duration-300 hover:border-white/20 hover:shadow-2xl">
      <div className="flex items-center justify-between">
        <span className="text-xs font-medium text-slate-400">{title}</span>
        <div className={`rounded-xl p-2.5 ${colorScheme.badgeBg}`}>
          <Icon className={colorScheme.iconColor} size={20} />
        </div>
      </div>
      <div className="mt-4 flex items-baseline gap-2">
        <span className="text-3xl font-black text-white tracking-tight">{count}</span>
        {trend && <span className="text-xs font-semibold text-emerald-400">{trend}</span>}
      </div>
    </div>
  );
}
```

---

## 10. دليل تحويل المكونات إلى Next.js App Router

عند نقل هذه الواجهة إلى مشروع **Next.js 15+ (App Router)**:

1. **تحديد مكونات العميل (`'use client'`):**
   - كافة الجداول التفاعلية، شرائط الفلاتر، كود التيليجرام، وسياق التواجد المباشر (`PresenceContext`) يجب أن تبدأ بتعليمة `'use client';` لأنها تعتمد على أحداث المتصفح وحزم الـ WebSockets.
2. **الصفحات الثابتة وخوادم الـ SSR:**
   - يمكن جعل صفحة تسجيل الدخول والهيكل العام الخارجي (`layout.tsx`) Server Components لضمان أقصى سرعة تحميل مبدئية وSEO ممتاز.
3. **تنظيم المجلدات المقترح:**
   ```
   src/
   ├── app/
   │   ├── layout.tsx             # Root layout with Fonts & Context Providers
   │   ├── page.tsx               # Main Dashboard App Shell
   │   ├── login/page.tsx         # Auth & Login View
   │   └── api/                   # Next.js Serverless Route Handlers
   ├── components/
   │   ├── header/                # TopHeader, LivePresence, SearchBar
   │   ├── tabs/                  # TabBar, PinnedTabs, PriorityStepper
   │   ├── filters/               # FilterToolbar, PresetPills, MyTasksToggle
   │   ├── announcement/          # PageAnnouncementBar, AnnouncementModal
   │   ├── grids/                 # StageDataGrid, TagmeTable, ReelsTable
   │   ├── modals/                # TelegramQrModal, UserEditModal
   │   └── common/                # StatCard, HistoryInput, CustomBadge
   ├── contexts/
   │   ├── AuthContext.tsx        # Authentication & Role State
   │   └── PresenceContext.tsx    # Live Team Presence & Realtime
   └── lib/
       ├── supabase.ts            # Client & Admin Supabase instances
       └── announcements.ts       # Announcement DB helpers
   ```

---
> 📝 **تم إنشاء هذا الملف ليكون مرجعاً تصميمياً وهندسياً متكاملاً لكل تفاصيل الواجهة والأزرار والتفاعلات بالداشبورد.**
